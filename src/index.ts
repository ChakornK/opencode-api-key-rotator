// Plugin entry — implementation follows in task 8
import type { Plugin } from "@opencode-ai/plugin";

export const NvidiaNimKeyRotator: Plugin = async () => {
  return {};
};

export default NvidiaNimKeyRotator;
